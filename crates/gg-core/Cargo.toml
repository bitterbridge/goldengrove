[package]
name = "gg-core"
version = "0.1.0"
edition = "2021"

[dependencies]
libm = "0.2"
rand = { version = "0.8", default-features = false }
rand_pcg = "0.3"
serde = { version = "1", features = ["derive"] }
