[package]
name = "gg-terrain"
version = "0.1.0"
edition = "2021"

[dependencies]
gg-core = { path = "../gg-core" }
gg-gen = { path = "../gg-gen" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
